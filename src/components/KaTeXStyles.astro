---
/*
 * Included only when frontmatter has:
 *
 *   mathjax: true
 *
 * The frontmatter key is intentionally kept as `mathjax` for backward
 * compatibility, although KaTeX is now the renderer.
 */
import 'katex/dist/katex.min.css';
import '@/styles/katex-rupee.css';
---
